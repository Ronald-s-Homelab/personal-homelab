teleport-cluster:
  image: ronaldmiranda/teleport-arm
  chartMode: custom
  clusterName: ronaldmiranda.com.br
  labels:
    app: teleport-cluster
  highAvailability:
    certManager:
      enabled: true
      issuerName: letsencrypt-prod
      issuerKind: ClusterIssuer
    replicaCount: 1
  extraVolumes:
    - name: teleport-secret
      secret:
        secretName: teleport-secret
  extraVolumeMounts:
    - name: teleport-secret
      mountPath: /mnt/secrets
  annotations:
    pod:
      "cluster-autoscaler.kubernetes.io/safe-to-evict": "true"
